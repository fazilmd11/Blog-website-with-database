<%- include('partials/header'); -%>
<h1>Home</h1>
<p><%= homeSC %></p>

<%# var myTruncatedString = myString.substring(0,length); %>

<% postsejs.forEach((post) => { %>
    <h1><%= post.title %></h1>
      <p><%= post.content.substring(0,100) %>... <a href="/posts/<%= post._id %>">Read more</a></p>
<% }); %>

<%- include('partials/footer'); -%>
